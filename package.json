{
  "name": "podbay-poc",
  "version": "1.0.0",
  "description": "PodBay POC — dynamic plugin composition with require() as the universal wiring layer",
  "main": "reverse-client.js",
  "scripts": {
    "start": "node reverse-client.js",
    "restart": "docker compose restart podbay",
    "rebuild:image": "docker compose up -d --build",
    "rebuild:app": "docker compose restart podbay && node index.js rebuild clubwpt-desktop"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
